--- plugins/wincmd.c	2025-12-26 20:34:47.727808918 +0100
+++ plugins/wincmd.c.patched	2025-12-26 20:35:23.978810633 +0100
@@ -205,7 +205,7 @@
 
     /* Default the image if unspecified. */
     if (wc->image == NULL)
-        wc->image = g_strdup("window-manager");
+        wc->image = g_strdup("user-desktop");
 
     /* Save construction pointers */
     wc->settings = settings;
