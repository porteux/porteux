#!/bin/bash

PRGNAM=nv-codec-headers

CWD=$(dirname "$0")
TMP=$MODULE_PATH/$PRGNAM
PKG=$TMP/package-$PRGNAM

set -e

[ -e $TMP ] && rm -rf $TMP
mkdir -p $TMP $PKG
cd $TMP

wget https://github.com/FFmpeg/nv-codec-headers/archive/refs/heads/master.tar.gz -O ${PRGNAM}.tar.gz
tar xf ${PRGNAM}.tar.gz
cd nv-codec-headers-master
VERSION=$(date -r . +%Y%m%d)

make -j${NUMBER_THREADS} PREFIX=/usr LIBDIR=lib${SYSTEM_BITS} DESTDIR=$PKG install

cd $PKG
makepkg ${MAKEPKG_FLAGS} $MODULE_PATH/packages/$PRGNAM-$VERSION-noarch-1_porteux.txz
